import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.ual.msc_soccer_project',
  appName: 'MSCSoccerProject',
  webDir: 'www'
};

export default config;
